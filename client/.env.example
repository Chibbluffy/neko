# Copy this file to .env and fill in your values.
# Vue CLI automatically reads .env at build time.

# GIPHY API key — get a free key at https://developers.giphy.com
# Without this, the GIF picker falls back to a shared demo key (100 req/hr).
VUE_APP_GIPHY_API_KEY=
